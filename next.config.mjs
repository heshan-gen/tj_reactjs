/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.topjobs.lk'], // Add external domains where your images are hosted
    },
};

export default nextConfig;
